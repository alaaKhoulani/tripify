import { AllExceptionsFilterFilter } from './all_exceptions_filter.filter';

describe('AllExceptionsFilterFilter', () => {
  it('should be defined', () => {
    expect(new AllExceptionsFilterFilter()).toBeDefined();
  });
});
